spec_version: "0.1.0"
name: ghost-supercomputer
version: 1.0.0
description: Ghost SuperComputer — $0 AI agent on Hugging Face Spaces

model:
  preferred: "github:gpt-4o-mini"
  fallback:
    - "anthropic:claude-3-haiku-20240307"
  constraints:
    temperature: 0.7
    max_tokens: 4096

tools:
  - cli
  - read
  - write
  - memory
  - search_docs
  - query_database
  - upload_file
  - send_notification

runtime:
  max_turns: 50
  timeout: 120

skills:
  - code-review
  - data-analysis
  - web-research

memory:
  backend: neon
  table: agent_memory
  max_entries: 10000

compliance:
  risk_level: low
  human_in_the_loop: false
  recordkeeping:
    audit_logging: true
    retention_days: 90
